'use strict';

/**
 * pizza-sur-commande service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::pizza-sur-commande.pizza-sur-commande');
